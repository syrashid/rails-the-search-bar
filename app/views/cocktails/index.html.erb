
<div class="container">
  <% @cocktails.each do |cocktail| %>
  <%= link_to cocktail.name, cocktail_path(cocktail), class: "btn btn-primary mt-3" %>
  <% end %>
</div>

<%= link_to "Add a New Drink",  new_cocktail_path, class: "btn btn-primary"%>

