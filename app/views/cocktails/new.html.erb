<h1>Add a Cocktail</h1>

<%= simple_form_for [@cocktail] do |f| %>
<%= f.input :name, :label_html => { :class => 'form-label' } %>
<%= f.input :photo %>
<%= f.input :photo_cache, as: :hidden %>
<%= f.submit "Submit Cocktail", class: "btn btn-dark" %>
<% end %>
